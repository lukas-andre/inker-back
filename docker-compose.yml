version: '3'
services:
  libretranslate:
    image: libretranslate/libretranslate
    ports:
      - "5000:5000"
    environment:
      - LT_API_KEYS=true
      - LT_LOAD_ONLY=en,es
    restart: unless-stopped
    volumes:
      - libretranslate-data:/app/db

volumes:
  libretranslate-data: 