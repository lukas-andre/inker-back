apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inker-back.fullname" . }}-test-connection"
  labels:
    {{- include "inker-back.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inker-back.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
