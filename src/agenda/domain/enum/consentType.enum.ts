export enum ConsentType {
  // Example types, to be defined as per actual requirements
  GENERAL_TERMS = 'GENERAL_TERMS',
  TATTOO_CONSENT = 'TATTOO_CONSENT',
  IMAGE_RELEASE = 'IMAGE_RELEASE',
  OTHER = 'OTHER',
}
