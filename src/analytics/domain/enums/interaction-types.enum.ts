export enum InteractionType {
  VIEW = 'view',
  LIKE = 'like',
  IMPRESSION = 'impression',
  CONVERSION = 'conversion',
  FOLLOW = 'follow',
}

export enum ViewSource {
  SEARCH = 'search',
  FEED = 'feed',
  PROFILE = 'profile',
  RELATED = 'related',
  DIRECT = 'direct',
}
